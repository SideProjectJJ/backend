spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE}
  application:
    name: pbl
  jpa:
    open-in-view: false

jwt:
  secret-key: ${JWT_SECRET_KEY}
  accessToken-expiration-millis: 1000000 # 추후 수정